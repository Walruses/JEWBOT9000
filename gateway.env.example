# IB Gateway login for docker-compose (keep private: chmod 600 gateway.env).
# Use the PAPER account's username (Client Portal > Settings > Paper Trading Account).
TWS_USERID=your_paper_username
TWS_PASSWORD=your_paper_password
# Password for VNC access to the Gateway screen (reach it through an SSH tunnel only)
VNC_SERVER_PASSWORD=choose_a_password
